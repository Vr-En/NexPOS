<UserControl x:Class="NexPOS.CategoriesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F3F4F6">

        <ScrollViewer VerticalScrollBarVisibility="Auto">

            <!-- FULL WIDTH CONTAINER -->
            <Grid Margin="24">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Categories"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#111827"/>

                    <TextBlock Text="Manage product categories"
                               FontSize="14"
                               Foreground="#64748B"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <!-- ADD / EDIT CATEGORY -->
                <Border Grid.Row="1"
                        Background="White"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,0,0,20">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock x:Name="txtFormTitle"
                                       Text="Add New Category"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       Foreground="#111827"
                                       Margin="0,0,0,12"/>

                            <TextBox x:Name="txtCategoryName"
                                     Height="38"
                                     FontSize="14"
                                     VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Bottom"
                                    Margin="12,0,0,0">

                            <Button x:Name="btnSaveCategory"
                                    Content="+ Add"
                                    Width="90"
                                    Height="38"
                                    Background="#00A651"
                                    Foreground="White"
                                    FontWeight="Bold"
                                    BorderThickness="0"
                                    Click="SaveCategory_Click"/>

                            <Button x:Name="btnCancelEdit"
                                    Content="Cancel"
                                    Width="90"
                                    Height="38"
                                    Margin="10,0,0,0"
                                    Background="#E5E7EB"
                                    Foreground="#111827"
                                    FontWeight="SemiBold"
                                    BorderThickness="0"
                                    Visibility="Collapsed"
                                    Click="CancelEdit_Click"/>
                        </StackPanel>

                    </Grid>
                </Border>

                <!-- CATEGORY LIST (FULL WIDTH FIX) -->
                <Border Grid.Row="2"
                        Background="White"
                        CornerRadius="12"
                        Padding="0">

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock x:Name="txtCategoryCount"
                                   Text="0 CATEGORIES"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#64748B"
                                   Margin="20,18,20,10"/>

                        <StackPanel x:Name="categoryListPanel"
                                    Grid.Row="1"/>

                    </Grid>

                </Border>

            </Grid>

        </ScrollViewer>

    </Grid>
</UserControl>